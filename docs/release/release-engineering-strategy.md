# Release Engineering Strategy

This document is retained as a compatibility pointer for older references.

The canonical release strategy lives in:

- `docs/architecture/release-strategy.md`

Agents must update the canonical document instead of duplicating release process content here.
